import { Button } from "./_components/ui/button";

export default function Home() {
  // const { data: session } = useSession()
  // console.log(session?.user !== undefined ? true : false)

  return (
    <div>
      <Button>Home Page</Button>
    </div>
  );
}
